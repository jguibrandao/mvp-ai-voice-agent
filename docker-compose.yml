services:
  agent:
    build: .
    command: python main.py dev
    env_file: .env.local
    volumes:
      - ./agent_config.json:/app/agent_config.json
      - metrics:/app/data
    environment:
      - METRICS_FILE=/app/data/metrics_data.json
    depends_on:
      - api

  api:
    build: .
    command: python api.py
    ports:
      - "8001:8001"
    volumes:
      - ./agent_config.json:/app/agent_config.json
      - metrics:/app/data
    environment:
      - METRICS_FILE=/app/data/metrics_data.json

  web:
    image: node:20-slim
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
    volumes:
      - ./web:/app
    environment:
      - VITE_API_URL=http://api:8001

volumes:
  metrics:
